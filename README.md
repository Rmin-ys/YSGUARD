<div dir="rtl">

# 🚀 YSGUARD (Tunnel Master Pro) 

یک اسکریپت جامع و قدرتمند برای راه‌اندازی تانل امن **WireGuard + UDP2RAW** با قابلیت‌های هوشمند مانیتورینگ و اطلاع‌رسانی تلگرام.

---

## ✨ قابلیت‌های کلیدی

* **Jitter Protection:** استفاده از UDP2RAW برای تغییر شکل ترافیک و جلوگیری از شناسایی.
* **Kernel Ops:** بهینه‌سازی پارامترهای هسته لینوکس و فعال‌سازی **TCP BBR**.
* **Smart Healer:** مانیتورینگ خودکار پینگ و ری‌استارت هوشمند تانل در صورت قطعی.
* **Daily Report:** گزارش خودکار میزان مصرف ترافیک روزانه به تلگرام.
* **Telegram Bot:** اعلان لحظه‌ای وضعیت تانل (Alerts) روی گوشی شما.

---

## 🛠 نحوه نصب و اجرا

برای اجرای اسکریپت، دستور زیر را در ترمینال سرور خود کپی کنید. این دستور همیشه آخرین نسخه را بدون کش دریافت می‌کند:

</div>

```bash
curl -sSL https://raw.githubusercontent.com/Rmin-ys/YSGUARD/refs/heads/main/deploy.sh -o deploy.sh && chmod +x deploy.sh && sudo ./deploy.sh
```


